import { View, Text } from "react-native";

import { SafeAreaScreen } from "../components";

export default function Menu() {
  return (
    <SafeAreaScreen>
      <Text>Menu</Text>
    </SafeAreaScreen>
  );
}
